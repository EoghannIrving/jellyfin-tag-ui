version: "3.8"
services:
  jellyfin-tag-ui:
    build: .
    container_name: jellyfin-tag-ui
    ports:
      - "8088:8080"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./.env:/app/.env:ro
    restart: unless-stopped
